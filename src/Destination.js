class Destination {
  constructor(id, lodgingCostPerDay, flightCostPerPerson) {
    this.id = id;
    this.lodgingCostPerDay = lodgingCostPerDay;
    this.flightCostPerPerson = flightCostPerPerson;
  }
}

module.exports = Destination;
